= @user.email
%table
  %thead
    %td Access ID
    %td Secret Key
  %tr
    %td= @user.id
    %td= @user.secret_key
%br
= render 'api_keys/list'

= link_to('Logout', destroy_user_session_path, method: :delete)
